{
  "name": "code-challenge-2",
  "version": "1.0.0",
  "description": "A simple web application to manage your guest list for events.",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "keywords": ["guests", "json-server"],
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/sngige/CODE-CHALLENGE-2.git"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
